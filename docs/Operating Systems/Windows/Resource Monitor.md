Live diagnostics tool that groups resource usage by process, shows which applications are over-consuming CPU or which service is hogging the disk. The “Network” tab is handy for spotting suspicious outbound connections or traffic spikes.

*CompTIA A+*
