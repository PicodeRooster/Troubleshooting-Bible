# Dev Log — May 3, 2026

Added two new Security entries today from my notes: Logic Bombs and Authentication Methods & Factors. Used the shell script to push them into the Security directory. Noticed the script doesn't write any frontmatter — just dumps the definition and slaps the CompTIA A+ tag at the bottom. That's going to be a problem later.

---

Been thinking about how the browser view is going to handle last-edited timestamps. The whole point of the offline format is that there's no server — so I can't just query file metadata on the fly. The browser won't have access to any of that once everything is zipped up.

I think the timestamp has to be written into the file itself at some point, either when the entry is created or when the bundle gets packed. Not sure exactly where that fits yet. Could be frontmatter, could be a separate manifest, could be something the shell script handles automatically.

Haven't worked out the JavaScript side of it either. Something has to read whatever timestamp gets stored and push it to an element in the browser view. Might also want some kind of visual indicator if an entry is getting stale — security content goes out of date fast.

Nothing decided yet. Need to think through the bundling step more before I commit to any particular approach.
